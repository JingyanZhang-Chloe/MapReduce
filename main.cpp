#include <iostream>

template<class T, class U>
T* map(U* array, size_t size_of_array, ) {
    T* new_array = new T[size_of_array];
}


int main()
{
    std::cout << "Hello, World!" << std::endl;
    return 0;
}
